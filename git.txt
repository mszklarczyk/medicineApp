git remote add origin https://github.com/mszklarczyk/medicineApp.git
cd %project folder%
git init		repo init
git status		
git add .		mark all repo files as staged
git commit -m "Your message about the commit"
git push origin yourbranchname

git pull origin master

git checkout -b <branch_name>